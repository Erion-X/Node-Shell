module.exports = function() {
  process.stdin.on('data', (data) => {

    let cmd = data.toString().trim()
    if () {


    process.stdout.write();
  }
  process.stdout.write('\nprompt > ');
  });

}
